<h1><%= title %></h1>

<ul>
  <% books.forEach(book => { %>
    <li>
      <strong><%= book.author %></strong> by <%= book.title %> Subject: <%= book.subject %> Price: <%= book.price %> 
      
    </li>
  <% }) %>
</ul>
